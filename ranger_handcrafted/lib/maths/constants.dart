import 'dart:math';

import 'vector.dart';

/// 0.00001
const epsilon = 0.00001;

/// degreesToRadians converts to radians, for example, 45.0 * degreesToRadians = radians
const degreesToRadians = pi / 180.0;

final Vector v1 = Vector.zero();
final Vector v2 = Vector.zero();
final Vector v3 = Vector.zero();
